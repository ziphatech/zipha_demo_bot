services:
  - name: test-service
    type: web
    runtime: node
    plan: free
    startCommand: npm start
    build:
      command: npm install
    region: oregon
    repo: https://github.com/your-username/your-repo.git
    branch: master
